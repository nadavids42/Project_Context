"""Domain enums, models, and ledger transition rules.

Implemented so far: `projects` (project lifecycle, FR-001) and `audit`
(minimal project audit trail). Ledger items, observations, and
reconciliation/transition rules are not yet implemented — see
docs/Project_Context_Product_Plan_v1.md Section 9 (data model) and
Section 10 (reconciliation and status transitions).
"""
